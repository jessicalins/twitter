<div class="container">
	<div class="row">
		<div class="col-lg-6">
			<h3><%= "#{@user.name} is following #{@following.count} people" %></h3><br>
			<%= render 'follow/listing', users: @following %>
		</div>
	</div>
</div>